#!/usr/bin/env bash

set -euo pipefail

SHELL_FUNCTIONS="${HOME}/.claude/litellm/shell-functions.zsh"

if [ ! -f "${SHELL_FUNCTIONS}" ]; then
  echo "Missing shell helper file: ${SHELL_FUNCTIONS}" >&2
  exit 1
fi

# shellcheck disable=SC1090
source "${SHELL_FUNCTIONS}"

cc "$@"
